'use server'

import { CreateCategoryParams } from "@/types"
import { handleError } from "../utils"

export const createUser = async (user:CreateCategoryParams) =>{

    try {
        
    } catch (error) {
        handleError(error)
        
    }
}